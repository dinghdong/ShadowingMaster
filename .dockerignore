# 后端镜像的构建上下文是**仓库根目录**（见 backend/Dockerfile 顶部说明）。
# 这里只放行 backend/ 与 packages/，其余一律排除 —— 否则本机 697MB 的
# backend/media、前端 node_modules、设计稿与本地数据库都会被塞进构建上下文。
#
# 注意：不要排除 packages/proxy_auto，requirements.txt 里有 -e ../packages/proxy_auto。

**/.git
**/.gitignore
**/.DS_Store
**/node_modules
**/__pycache__
**/*.pyc
**/.venv
**/venv
**/.env
**/.env.*
**/.vercel
**/.cache
**/.workbuddy
**/test-results
**/playwright-report

# 本地运行时数据：生产由挂载卷（DATA_DIR=/data）与 OSS 提供，不进镜像
app.db
app.db.bak-*
app.db-journal
app.db-wal
app.db-shm
backend/media/
backend/data/
.tmp_video31_backup/

# 与后端镜像无关的大块内容
docs/
frontend/
knowledge.json
knowledge.decoded.json
knowledge.sample.json
*.cookies.txt
packages/proxy_auto/proxy_auto.egg-info/
