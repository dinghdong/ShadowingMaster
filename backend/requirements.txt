fastapi
uvicorn[standard]
pydantic
python-jose[cryptography]
passlib[bcrypt]
python-multipart
yt-dlp
curl_cffi  # 启用 yt-dlp TLS 客户端指纹伪装（impersonation）；缺失时 ANDROID_VR 等客户端连接被 YouTube CDN 间歇性 TLS 重置（SSL UNEXPECTED_EOF），见 backend/fetch_video.py
requests
aiofiles
oss2  # 阿里云 OSS 媒体存储（部署到阿里云时使用；缺失时自动降级为本地磁盘，见 backend/core/oss.py）
